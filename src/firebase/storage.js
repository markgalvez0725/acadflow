// Intentionally empty. Firebase Storage uploads were removed to avoid the
// Blaze (billing) requirement. Student submissions use links only. This file
// is not imported anywhere; kept as a no-op placeholder.
export {}
