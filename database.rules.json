{
  "rules": {
    ".read": false,
    ".write": false,

    "probe": {
      "$id": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "rooms": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
