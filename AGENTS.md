# AcadFlow is NOT a Next.js project

This is a **React + Vite** app. There is no App Router, no `pages/` directory, no server components, and no `next/*` imports. See `CLAUDE.md` for the full agent guide.
